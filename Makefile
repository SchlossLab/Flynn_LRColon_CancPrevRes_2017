# Makefile for "Spatial Variation of the Native Human Colonic Microbiota" Flynn, KJ et al 2017

# Set local variables
REFS = data/references
FIGS = results/figures
TABLES = data/process/tables
PROC = data/process
FINAL = submission
CODE = code
METADATA = data/raw/metadata


### to write rules for 

## pulling in/updating silva files

## running mothur commands (with splits at cluster.split as appropriate)

## running R commands or build_figures

##where do I put other HPC stuff? like the random forest models 

# write.paper / Render the manuscript 